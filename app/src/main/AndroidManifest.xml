<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp304.josephharrisonlim.assignment2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop"/>
        <activity
            android:name=".foods.VegetablesActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".foods.FruitsActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".foods.GrainsActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".foods.MeatsActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".foods.DairyActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".FinalOrderActivity"
            android:parentActivityName=".MenuActivity" />
    </application>

</manifest>